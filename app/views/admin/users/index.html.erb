<% provide(:title, 'All users') %>
<h1>All users</h1>
<h1>
<%= @users.all.count %>
</h1>
<%= link_to "Add New Users", new_admin_user_path, class: "btn btn-primary" %>
<li class="divider" ></li>
<%= will_paginate %>
<ul class="users" >
  <%= render @users %>
</ul>
</div>
<%= will_paginate %>